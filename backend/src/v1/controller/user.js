const user=require('../models/user')
const CryptoJS=require('crypto-js')
const jsonwebtoken=require('jsonwebtoken')

exports.register=async(req,res)=>{
    // const {password}=<req className="body">try{
    // req.body.password=
}